# Skopiuj ten plik jako .env i uzupełnij wartości.
GEMINI_API_KEY=twoj_klucz_api_tutaj
GEMINI_MODEL=gemini-2.0-flash

# Limit (w sekundach) na pojedyncze wywołanie API Gemini.
# Analiza + automatyczna samopoprawa wykonuje kilka wywołań, więc warto trzymać wyższy.
GEMINI_TIMEOUT=180
